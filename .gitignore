.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store
htmlcov/
.coverage
.vagrant/
infrastructure/vagrant/config/vagrant.env
node_modules/
infrastructure/docker/deployment

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
infrastructure/terraform/terraform.tfvars
infrastructure/terraform/legacy
infrastructure/legacy_terraform
infrastructure/docker/deployment
*.tfvars
*.tfvars.json
*.tfplan
**/tfplan
crash.log
crash.*.log
*.json
!infrastructure/terraform/*.json
# Credentials and private keys
*.pem
*.key
!project-config.example.json

# Built Ansible collection artifacts
infrastructure/ansible/**/*.tar.gz
infrastructure/ansible/inventory/oilscope.gcp.yml

*.local.gcp.yml

# Local backups of tfvars
*.tfvars.bak

# Local environment: paths and credential-store lookups, never values.
.envrc

# Collection path for local runs. Only the symlink to this repository's own
# collection belongs in git; anything Galaxy installs beside it does not.
.ansible/collections/ansible_collections/*
!.ansible/collections/ansible_collections/oilscope
